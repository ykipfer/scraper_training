To run this script you first need to install playwright

```bash
pip install playwright
```

After installing playwright via pip you need to run the following command to install the browsers

```bash
playwright install
```